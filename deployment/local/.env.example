# =============================================================================
# AI Assistant Platform - Local Deployment Environment
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#   nano .env
#
# NEVER commit .env to git!
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE (PostgreSQL)
# -----------------------------------------------------------------------------
POSTGRES_USER=platform
POSTGRES_PASSWORD=your-secure-database-password-change-this
POSTGRES_DB=platform

# -----------------------------------------------------------------------------
# CACHE (Redis)
# -----------------------------------------------------------------------------
REDIS_PASSWORD=your-secure-redis-password-change-this

# -----------------------------------------------------------------------------
# SESSION & SECURITY
# -----------------------------------------------------------------------------
# Generate with: openssl rand -base64 32
SESSION_SECRET=your-session-secret-at-least-32-characters-long

# -----------------------------------------------------------------------------
# AI PROVIDERS
# -----------------------------------------------------------------------------
# OpenAI (for GPT-4o, GPT-4o-mini)
OPENAI_API_KEY=sk-your-openai-key

# Anthropic (for Claude)
ANTHROPIC_API_KEY=sk-ant-your-anthropic-key

# Google AI (for Gemini - REQUIRED for LiveKit Agent)
GOOGLE_API_KEY=your-google-ai-key

# Voyage AI (for embeddings)
VOYAGE_API_KEY=your-voyage-key

# -----------------------------------------------------------------------------
# LIVEKIT (WebRTC)
# -----------------------------------------------------------------------------
# Generate keys with: docker run --rm livekit/livekit-server generate-keys
LIVEKIT_API_KEY=your-livekit-api-key
LIVEKIT_API_SECRET=your-livekit-api-secret

# -----------------------------------------------------------------------------
# CLOUDFLARE TUNNEL
# -----------------------------------------------------------------------------
# Get this from Cloudflare Zero Trust Dashboard:
# 1. Go to cloudflare.com → Zero Trust → Networks → Tunnels
# 2. Create a tunnel
# 3. Copy the token
CLOUDFLARE_TUNNEL_TOKEN=your-cloudflare-tunnel-token

# -----------------------------------------------------------------------------
# PUBLIC URLs (Update with your domain)
# -----------------------------------------------------------------------------
# These are the public-facing URLs after Cloudflare Tunnel
PUBLIC_API_URL=https://api.yourdomain.com
PUBLIC_WS_URL=wss://ws.yourdomain.com
PUBLIC_LIVEKIT_URL=wss://livekit.yourdomain.com

# CORS - comma separated origins (or * for development)
CORS_ORIGIN=https://yourdomain.com,https://app.yourdomain.com,https://meet.yourdomain.com

# -----------------------------------------------------------------------------
# OPTIONAL: OAuth Providers
# -----------------------------------------------------------------------------
# Google OAuth
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Microsoft OAuth
MICROSOFT_CLIENT_ID=
MICROSOFT_CLIENT_SECRET=

# -----------------------------------------------------------------------------
# OPTIONAL: Monitoring
# -----------------------------------------------------------------------------
# Sentry DSN for error tracking
SENTRY_DSN=
