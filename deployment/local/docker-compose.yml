version: '3.8'

# =============================================================================
# IFinallyWill - Local Server Deployment
# =============================================================================
# Usage:
#   docker compose up -d          # Start all services
#   docker compose down           # Stop all services
#   docker compose logs -f        # View logs
#   docker compose ps             # Check status
# =============================================================================

name: ifinallywill

services:
  # ===========================================================================
  # DATABASES
  # ===========================================================================

  postgres:
    image: pgvector/pgvector:pg16
    container_name: ifinallywill-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-ifinallywill}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Database password required}
      POSTGRES_DB: ${POSTGRES_DB:-ifinallywill}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./config/postgres/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-ifinallywill} -d ${POSTGRES_DB:-ifinallywill}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    networks:
      - ifinallywill-network

  redis:
    image: redis:7.4-alpine
    container_name: ifinallywill-redis
    restart: unless-stopped
    command: >
      redis-server
      --requirepass ${REDIS_PASSWORD:?Redis password required}
      --appendonly yes
      --maxmemory 256mb
      --maxmemory-policy allkeys-lru
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - ifinallywill-network

  # ===========================================================================
  # LIVEKIT (WebRTC Server)
  # ===========================================================================

  livekit:
    image: livekit/livekit-server:v1.7
    container_name: ifinallywill-livekit
    restart: unless-stopped
    command: --config /etc/livekit.yaml
    volumes:
      - ./config/livekit/livekit.yaml:/etc/livekit.yaml:ro
    ports:
      - "7880:7880"     # HTTP API
      - "7881:7881"     # WebRTC TCP
      - "7882:7882/udp" # WebRTC UDP (TURN)
      - "50000-50060:50000-50060/udp"  # RTC UDP ports
    environment:
      LIVEKIT_KEYS: "${LIVEKIT_API_KEY}:${LIVEKIT_API_SECRET}"
    networks:
      - ifinallywill-network

  # ===========================================================================
  # BACKEND SERVICES
  # ===========================================================================

  api:
    build:
      context: ../..
      dockerfile: deployment/local/dockerfiles/api.Dockerfile
    container_name: ifinallywill-api
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 3001
      DATABASE_URL: postgresql://${POSTGRES_USER:-ifinallywill}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-ifinallywill}
      REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379
      SESSION_SECRET: ${SESSION_SECRET:?Session secret required}
      # AI Provider Keys
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      GOOGLE_API_KEY: ${GOOGLE_API_KEY:-}
      VOYAGE_API_KEY: ${VOYAGE_API_KEY:-}
      # LiveKit
      LIVEKIT_URL: http://livekit:7880
      LIVEKIT_API_KEY: ${LIVEKIT_API_KEY}
      LIVEKIT_API_SECRET: ${LIVEKIT_API_SECRET}
      # CORS
      CORS_ORIGIN: ${CORS_ORIGIN:-*}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3001/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - ifinallywill-network

  realtime:
    build:
      context: ../..
      dockerfile: deployment/local/dockerfiles/realtime.Dockerfile
    container_name: ifinallywill-realtime
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 3002
      REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379
      SESSION_SECRET: ${SESSION_SECRET}
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3002/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - ifinallywill-network

  # ===========================================================================
  # LIVEKIT AGENT (Python)
  # ===========================================================================

  livekit-agent:
    build:
      context: ../..
      dockerfile: deployment/local/dockerfiles/livekit-agent.Dockerfile
    container_name: ifinallywill-livekit-agent
    restart: unless-stopped
    environment:
      LIVEKIT_URL: ws://livekit:7880
      LIVEKIT_API_KEY: ${LIVEKIT_API_KEY}
      LIVEKIT_API_SECRET: ${LIVEKIT_API_SECRET}
      GOOGLE_API_KEY: ${GOOGLE_API_KEY:?Google API key required for Gemini}
      # Backend API for context
      BACKEND_API_URL: http://api:3001
    depends_on:
      - livekit
      - api
    networks:
      - ifinallywill-network

  # ===========================================================================
  # FRONTEND APPS
  # ===========================================================================

  landing:
    build:
      context: ../..
      dockerfile: deployment/local/dockerfiles/landing.Dockerfile
      args:
        VITE_API_URL: ${PUBLIC_API_URL:-https://api.yourdomain.com}
    container_name: ifinallywill-landing
    restart: unless-stopped
    networks:
      - ifinallywill-network

  dashboard:
    build:
      context: ../..
      dockerfile: deployment/local/dockerfiles/dashboard.Dockerfile
      args:
        VITE_API_URL: ${PUBLIC_API_URL:-https://api.yourdomain.com}
        VITE_WS_URL: ${PUBLIC_WS_URL:-wss://ws.yourdomain.com}
    container_name: ifinallywill-dashboard
    restart: unless-stopped
    networks:
      - ifinallywill-network

  meeting:
    build:
      context: ../..
      dockerfile: deployment/local/dockerfiles/meeting.Dockerfile
      args:
        VITE_API_URL: ${PUBLIC_API_URL:-https://api.yourdomain.com}
        VITE_LIVEKIT_URL: ${PUBLIC_LIVEKIT_URL:-wss://livekit.yourdomain.com}
    container_name: ifinallywill-meeting
    restart: unless-stopped
    networks:
      - ifinallywill-network

  # ===========================================================================
  # REVERSE PROXY
  # ===========================================================================

  nginx:
    image: nginx:alpine
    container_name: ifinallywill-nginx
    restart: unless-stopped
    volumes:
      - ./config/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "80:80"
    depends_on:
      - api
      - realtime
      - landing
      - dashboard
      - meeting
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - ifinallywill-network

  # ===========================================================================
  # CLOUDFLARE TUNNEL
  # ===========================================================================

  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: ifinallywill-tunnel
    restart: unless-stopped
    command: tunnel run
    environment:
      TUNNEL_TOKEN: ${CLOUDFLARE_TUNNEL_TOKEN:?Cloudflare tunnel token required}
    depends_on:
      - nginx
    networks:
      - ifinallywill-network

# =============================================================================
# VOLUMES
# =============================================================================

volumes:
  postgres_data:
    name: ifinallywill-postgres-data
  redis_data:
    name: ifinallywill-redis-data

# =============================================================================
# NETWORKS
# =============================================================================

networks:
  ifinallywill-network:
    name: ifinallywill-network
    driver: bridge
