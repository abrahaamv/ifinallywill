/**
 * Real-time Package (Phase 6)
 *
 * WebSocket server with Redis Streams for multi-instance chat
 */

export { RealtimeServer, MessageType } from './websocket-server';
export type { WSMessage } from './websocket-server';
