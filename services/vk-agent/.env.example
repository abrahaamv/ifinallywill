# VK-Agent Environment Configuration
# Copy to .env and fill in values

# ============================================================
# REQUIRED: Gemini API Key
# ============================================================
GEMINI_API_KEY=your_gemini_api_key_here

# ============================================================
# Janus Configuration
# ============================================================
# WebSocket URL for Janus Gateway
VK_AGENT_JANUS_WS_URL=ws://localhost:8188

# AudioBridge room ID
VK_AGENT_JANUS_ROOM_ID=5679

# Display name in room
VK_AGENT_JANUS_DISPLAY=VKAgent

# RTP host - where Janus will send audio TO us
# If Janus is in Docker, use Docker gateway (typically 172.19.0.1)
# If Janus is on same host, use 127.0.0.1
VK_AGENT_RTP_HOST=172.19.0.1

# RTP port - UDP port for receiving audio from Janus
VK_AGENT_RTP_PORT=5004

# ============================================================
# Gemini Configuration
# ============================================================
# Model ID (stable: models/gemini-2.0-flash-exp)
VK_AGENT_GEMINI_MODEL=models/gemini-2.0-flash-exp

# Voice preset (options: Puck, Aoede, Charon, Fenrir, Kore)
VK_AGENT_GEMINI_VOICE=Puck

# System instruction (multi-line example)
# VK_AGENT_SYSTEM_INSTRUCTION=You are a helpful AI assistant in a voice meeting.

# ============================================================
# Logging & Debug
# ============================================================
# Log level (DEBUG, INFO, WARNING, ERROR)
VK_AGENT_LOG_LEVEL=INFO

# Save audio to files for debugging (true/false)
VK_AGENT_DEBUG_AUDIO=false

# Directory for debug audio files
VK_AGENT_DEBUG_AUDIO_DIR=/tmp/vk-agent-audio

# ============================================================
# API Server (optional)
# ============================================================
VK_AGENT_API_HOST=0.0.0.0
VK_AGENT_API_PORT=3004
