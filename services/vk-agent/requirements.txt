# VK-Agent Dependencies
# Voice AI Bridge: Janus AudioBridge <-> Gemini Live API
#
# Phase 1 Optimizations:
#   - soxr: High-performance resampling (40-80ms latency savings)
#   - pybreaker: Circuit breaker for API resilience
#   - tenacity: Retry with exponential backoff
#   - torch + silero-vad: Voice Activity Detection (optional, 30-50ms savings)

# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0

# WebSocket client for Janus and Gemini
websockets==14.1

# Google Gemini (unused - we use raw WebSocket)
# google-genai==0.1.0

# Audio processing
numpy==2.2.2
av==13.1.0
opuslib==3.0.1
soxr==0.5.0.post1         # Phase 1: High-performance resampling (replaces scipy)
# scipy==1.15.0           # Optional fallback if soxr unavailable

# Reliability (Phase 1)
pybreaker==1.2.0          # Circuit breaker pattern
tenacity==9.0.0           # Retry with backoff

# Image/Video processing
Pillow==11.0.0

# Async utilities
aiohttp==3.11.11

# Environment
python-dotenv==1.0.1

# Logging
structlog==24.4.0

# Voice Activity Detection (Phase 1)
# Silero VAD: 87.7% true positive rate, sub-millisecond processing
torch>=2.5.0              # Required for Silero VAD (use latest compatible)
torchaudio>=2.5.0         # Audio utilities for torch (use latest compatible)
packaging>=21.0           # Required by Silero VAD
