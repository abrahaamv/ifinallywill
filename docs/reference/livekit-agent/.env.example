# AI Assistant Platform - Production Agent Configuration
# Copy this file to .env and fill in your actual values

# =============================================================================
# LiveKit Configuration (REQUIRED)
# =============================================================================
# LiveKit Cloud or self-hosted server URL
LIVEKIT_URL=wss://your-livekit-server.livekit.cloud

# LiveKit API credentials (from LiveKit Cloud dashboard)
LIVEKIT_API_KEY=your-api-key
LIVEKIT_API_SECRET=your-api-secret

# =============================================================================
# AI Service Providers - COST-OPTIMIZED ARCHITECTURE (80% Cost Reduction)
# =============================================================================

# --- Vision Providers (Gemini Flash 85% + Claude Sonnet 15%) ---
# Google Gemini API key for routine vision analysis (85% of frames, $0.10/1M tokens)
GOOGLE_API_KEY=your-google-api-key

# Anthropic Claude API key for complex vision analysis (15% of frames, $3/1M tokens)
ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key

# --- LLM Providers (GPT-4o-mini 70% + GPT-4o 30%) ---
# OpenAI API key for LLM routing (GPT-4o-mini: 70%, GPT-4o: 30%)
OPENAI_API_KEY=sk-your-openai-api-key

# --- Speech-to-Text Provider ---
# Deepgram API key for speech-to-text (Nova-2 model)
DEEPGRAM_API_KEY=your-deepgram-api-key

# --- Text-to-Speech Providers (ElevenLabs primary + Cartesia fallback) ---
# ElevenLabs API key for premium voice quality (best emotional warmth)
ELEVENLABS_API_KEY=your-elevenlabs-api-key

# Cartesia API key for fallback TTS (fast and reliable)
CARTESIA_API_KEY=your-cartesia-api-key

# --- Embeddings Provider (Knowledge Enhancement) ---
# Voyage AI API key for multimodal embeddings (1024 dimensions, $0.10/1M tokens)
VOYAGE_API_KEY=your-voyage-api-key

# =============================================================================
# Backend API Integration (REQUIRED)
# =============================================================================
# TypeScript tRPC backend URL
API_BASE_URL=http://localhost:3001

# Agent API key for backend authentication
AGENT_API_KEY=your-agent-api-key

# =============================================================================
# Environment Settings
# =============================================================================
# Environment: development | staging | production
AGENT_ENV=development

# =============================================================================
# Configuration Guide
# =============================================================================
# 1. LiveKit Setup:
#    - Sign up at https://livekit.io
#    - Create project and get API credentials
#    - Use cloud URL or deploy self-hosted server
#
# 2. AI Services (Cost-Optimized Provider System):
#    VISION PROVIDERS (80% cost reduction):
#      - Google Gemini: https://makersuite.google.com/app/apikey (routine analysis)
#      - Anthropic Claude: https://console.anthropic.com (complex analysis)
#
#    LLM PROVIDERS (80% cost reduction):
#      - OpenAI: https://platform.openai.com/api-keys (GPT-4o-mini + GPT-4o)
#
#    SPEECH PROVIDERS:
#      - Deepgram: https://console.deepgram.com (speech-to-text)
#      - ElevenLabs: https://elevenlabs.io/app/settings (premium TTS)
#      - Cartesia: https://cartesia.ai (fallback TTS)
#
#    EMBEDDINGS PROVIDER:
#      - Voyage AI: https://www.voyageai.com (knowledge embeddings)
#
# 3. Backend Integration:
#    - Set API_BASE_URL to your tRPC backend (default: http://localhost:3001)
#    - Generate AGENT_API_KEY in backend admin panel
#    - Backend provides: tenant context, usage tracking, knowledge search
#
# 4. Environment-Specific Configuration:
#    Development:
#      - Minimum: OPENAI_API_KEY, DEEPGRAM_API_KEY, CARTESIA_API_KEY
#      - Optional: GOOGLE_API_KEY, ANTHROPIC_API_KEY (for cost optimization testing)
#      - AGENT_ENV=development
#
#    Production:
#      - Required: All provider keys for full cost optimization
#      - GOOGLE_API_KEY + ANTHROPIC_API_KEY = 80% vision cost reduction
#      - OPENAI_API_KEY = 80% LLM cost reduction (mini + full routing)
#      - ELEVENLABS_API_KEY = Best quality TTS for elder users
#      - VOYAGE_API_KEY = Enhanced knowledge search
#      - AGENT_ENV=production
#      - Estimated savings: $680K annually at 100K sessions
#
# 5. Documentation References:
#    - docs/AI_AGENT_REFACTORING_COMPLETE.md: Provider system implementation
#    - docs/INTEGRATION_GUIDE.md: Backend integration patterns
#    - docs/SETUP.md: Quick start guide
#    - providers/README.md: Provider abstraction architecture (if exists)
#
# 6. Cost Optimization Features:
#    - Vision routing: Complexity-based Gemini/Claude selection (85/15 split)
#    - LLM routing: Complexity-based GPT-4o-mini/GPT-4o selection (70/30 split)
#    - Automatic provider fallback for reliability
#    - Real-time usage tracking per provider
#    - Cost analytics and optimization recommendations
